// The relative-table macros, for lwIP's patched tcp.c. A separate header so the
// vendored source needs only a name it can find on the include path, and does
// not have to know where UbiqOS keeps its ABI.
#ifndef UBIQOS_RELTAB_H
#define UBIQOS_RELTAB_H
#include "../../common/ubiqos_abi.h"
#endif
